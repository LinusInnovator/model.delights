# 🚀 __PROJECT_NAME__

Welcome to your **Zero-Latency Global Routing Architecture**, mathematically compiled and generated by the **model.delights.pro CTO Factory**.

This repository contains a pristine, highly-optimized **Next.js 14 App Router** application pre-wired with the exact micro-service specifications you requested. Rather than using generic, expensive foundation models for every task, your architecture routes distinct semantic intents to the absolute highest-ELO specialized models across global providers.

---

## 🛠️ Step 1: Initialize the Environment

Your architecture strictly requires API keys for the providers mapped in your blueprint.

1. Locate the `.env.example` file in the root of this project.
2. Duplicate it and rename the copy to `.env.local`:
   ```bash
   cp .env.example .env.local
   ```
3. Open `.env.local` and paste in your production API keys for the specified providers (e.g., Anthropic, OpenAI, Google, Groq).

> **Crucial Setup Note:** Without these keys, the Edge Routing functions will fail securely.

## 📦 Step 2: Install Node Dependencies

This boilerplate is powered by the **Vercel AI SDK**, **React 18**, and **Tailwind CSS**. 

Open your terminal in this directory and execute:
```bash
npm install
```

## ⚡️ Step 3: Launch the Intelligence Engine

Once your packages are installed and `.env.local` is populated, ignite the local development server:

```bash
npm run dev
```

Navigate to **[http://localhost:3000](http://localhost:3000)** in your browser. 
You will see a premium, dark-mode terminal chat interface ready to accept user input.

---

## 🏗️ Architecture Breakdown

### 1. `src/app/page.tsx`
This is your React Client. It handles the `useChat` hook, rendering the dark-mode UI, message history, and streaming the chunks from your API as they resolve.

### 2. `src/app/api/chat/route.ts`
This is the **Core Brain**. Our AST (Abstract Syntax Tree) Injector has specifically tailored this `/api/chat` endpoint to match your blueprint. It contains the exact `streamText` logic routing to your chosen model ID.

## 🚀 Step 4: Deploying to Production (Vercel)

This architecture is deeply integrated with Next.js App Router and Edge Functions. The best place to deploy it is **Vercel**.

1. Push this repository to a private **GitHub**, **GitLab**, or **Bitbucket** account.
2. Go to [Vercel.com](https://vercel.com/) and click **"Add New Project"**.
3. Import your repository.
4. **CRITICAL:** In the Vercel deployment settings, expand "Environment Variables" and paste in the keys from your `.env.local` file.
5. Click **Deploy**.

Within 60 seconds, your Zero-Latency Application will be live globally.

---
*Generated autonomously by the [model.delights.pro](https://model.delights.pro/) Super-Architect.*
